import { WesDetailsComponent } from './wes-details.component';

describe('WesDetailsComponent', () => {
  let component: WesDetailsComponent;

  beforeEach(() => {
    component = new WesDetailsComponent(null, null, null, null, null, null);
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
