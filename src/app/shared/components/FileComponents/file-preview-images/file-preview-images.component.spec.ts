import { FilePreviewImagesComponent } from './file-preview-images.component';

describe('FilePreviewImagesComponent', () => {
  let component: FilePreviewImagesComponent;

  beforeEach(() => {
    component = new FilePreviewImagesComponent(null);
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
