import { AttributeOptionMessageDialogComponent } from './attribute-option-message-dialog.component';

describe('AttributeOptionMessageDialogComponent', () => {
  let component: AttributeOptionMessageDialogComponent;

  beforeEach(() => {
    component = new AttributeOptionMessageDialogComponent(null, null, null, null);
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
