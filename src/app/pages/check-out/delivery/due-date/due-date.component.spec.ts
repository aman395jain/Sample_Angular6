import { DueDateComponent } from './due-date.component';

describe('DueDateComponent', () => {
  let component: DueDateComponent;

  beforeEach(() => {
    component = new DueDateComponent(null);
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
