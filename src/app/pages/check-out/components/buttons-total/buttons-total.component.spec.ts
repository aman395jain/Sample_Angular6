import { ButtonsTotalComponent } from './buttons-total.component';

describe('ButtonsTotalComponent', () => {
  let component: ButtonsTotalComponent;

  beforeEach(() => {
    component = new ButtonsTotalComponent(null, null, null, null, null, null, null, null, null, null, null);
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
