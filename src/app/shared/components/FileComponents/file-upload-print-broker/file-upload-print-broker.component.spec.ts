import { FileUploadPrintBrokerComponent } from './file-upload-print-broker.component';

describe('FileUploadPrintBrokerComponent', () => {
  let component: FileUploadPrintBrokerComponent;

  beforeEach(() => {
    component = new FileUploadPrintBrokerComponent(null, null, null, null, null, null, null, null);
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
