import { FilePreviewModalWrapperComponent } from './file-preview-modal-wrapper.component';

describe('FilePreviewModalWrapperComponent', () => {
  let component: FilePreviewModalWrapperComponent;

  beforeEach(() => {
    component = new FilePreviewModalWrapperComponent(null, null, null, null);
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
