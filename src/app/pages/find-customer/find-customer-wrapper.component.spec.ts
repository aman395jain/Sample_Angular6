import { FindCustomerWrapperComponent } from './find-customer-wrapper.component';

describe('FindCustomerWrapperComponent', () => {
  let component: FindCustomerWrapperComponent;

  beforeEach(() => {
    component = new FindCustomerWrapperComponent(null, null, null, null, null, null);
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
