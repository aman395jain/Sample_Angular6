import { ConfigSummaryComponent } from './ConfigSummary.component';

describe('ConfigSummaryComponent', () => {
  let component: ConfigSummaryComponent;

  beforeEach(() => {
    component = new ConfigSummaryComponent(null, null, null, null, null, null, null, null, null, null, null, null, null, null);
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
