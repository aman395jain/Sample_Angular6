import { RewardsEnrollmentDialogComponent } from './rewards-enrollment-dialog.component';

describe('RewardsEnrollmentDialogComponent', () => {
  let component: RewardsEnrollmentDialogComponent;

  beforeEach(() => {
    component = new RewardsEnrollmentDialogComponent(null, null);
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
