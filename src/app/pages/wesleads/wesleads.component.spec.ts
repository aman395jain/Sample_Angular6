import { WesleadsComponent } from './wesleads.component';

describe('WesleadsComponent', () => {
  let component: WesleadsComponent;

  beforeEach(() => {
    component = new WesleadsComponent(null, null, null, null, null, null, null);
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
