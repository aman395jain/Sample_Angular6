import { SaveforlaterCustomerDialogComponent } from './saveforlater-customer-dialog.component';

describe('SaveforlaterCustomerDialogComponent', () => {
  let component: SaveforlaterCustomerDialogComponent;

  beforeEach(() => {
    component = new SaveforlaterCustomerDialogComponent(null, null, null, null, null, null, null);
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
