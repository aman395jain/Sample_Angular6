import { BackgroundTaskStatusComponent } from './background-task-status.component';

describe('BackgroundTaskStatusComponent', () => {
  let component: BackgroundTaskStatusComponent;

  beforeEach(() => {
    component = new BackgroundTaskStatusComponent(null);
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
